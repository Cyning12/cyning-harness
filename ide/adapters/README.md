# ide/adapters

**单源 POINTER**：真值在 `coding_wiki/` + `standards/`；本目录仅 IDE 入口片段。

## 文件（v0.1 待补）

| 勾选 | 输出 |
|------|------|
| Cursor | `.cursor/rules/harness-pointer.mdc` |
| Claude Code | `CLAUDE.md.fragment` |
| 通用 | `AGENTS.md.fragment` |

由 `wizard/` 合并写入用户仓根或约定路径。
